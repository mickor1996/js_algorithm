/**
 * 把所有询问的答案按询问顺序放入vector里
 * @param arr int整型一维数组 要查询坐标的数组
 * @return int整型一维数组
 */
function MinimumTimes(arr) {
  // write code here
  let temp = [0, 3, , 2, 2,]
  for (let i = 0; i < arr.length; i++) {
    arr[i]
  }
}
module.exports = {
  MinimumTimes: MinimumTimes
};